Dans les cas où une information manque, on peut mettre nil 

```swift


var age: Int? = nil //on utilise cette syntaxe quand on ne sait pas quelle valeur stocker à ce stade dans une variable comme age.

